# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
gunicorn==23.0.0
starlette==0.38.6
python-multipart==0.0.12
websockets==13.1

# Pydantic / settings
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Database
sqlalchemy==2.0.35
asyncpg==0.29.0
# Sync driver: code uses psycopg (v3) via "postgresql+psycopg" URL
psycopg[binary]==3.2.3
alembic==1.13.3

# Cache / queue
redis==5.1.1
celery==5.4.0

# HTTP clients
httpx==0.27.2
aiohttp==3.10.10
aiofiles==24.1.0
requests==2.32.3

# Auth
python-jose[cryptography]==3.3.0
Authlib==1.3.2

# DigitalOcean Spaces (S3 compatible)
boto3==1.35.36
botocore==1.35.36

# AI / TTS
google-generativeai==0.8.3
google-genai==2.20.0
gTTS==2.5.3

# Audio processing
librosa==0.10.2.post1
numpy==1.26.4
scipy==1.13.1
pandas==2.2.3
pydub==0.25.1
SpeechRecognition==3.10.4
webrtcvad==2.0.10

# Text analysis
textstat==0.7.4

# Payments (legacy PayPal SDK; kept for billing plan/agreement code paths)
paypalrestsdk==1.13.3
# NOTE: python-paypal-api removed (the version pinned in code does not exist on
# PyPI; paypal_service.py now lazy-imports it and the app falls back gracefully).
# RevenueCat is the going-forward billing system.

# Native mobile sign-in (Google ID token + Apple)
google-auth==2.34.0
PyJWT[crypto]==2.9.0
